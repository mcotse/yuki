# ===========================================
# REQUIRED - Twilio Configuration
# ===========================================
TWILIO_ACCOUNT_SID=your_account_sid
TWILIO_AUTH_TOKEN=your_auth_token
# WhatsApp sandbox number (optional, defaults to Twilio sandbox)
TWILIO_WHATSAPP_NUMBER=+14155238886
# Recipient phone numbers (comma-separated)
TWILIO_RECIPIENT_NUMBERS=+1234567890,+0987654321

# ===========================================
# REQUIRED - Upstash Redis (for data storage)
# ===========================================
UPSTASH_REDIS_REST_URL=your_upstash_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_token

# ===========================================
# REQUIRED IN PRODUCTION - Security Settings
# ===========================================
# Secret for authenticating cron job requests (REQUIRED in production)
CRON_SECRET=your_random_secret_string

# API secret for authenticating write operations from the dashboard
# Used for POST/PUT/DELETE requests to /api/pending and /api/medications
API_SECRET=your_api_secret_key

# Allowed origins for CORS (comma-separated)
# Example: https://your-app.vercel.app,https://your-domain.com
ALLOWED_ORIGINS=https://your-app.vercel.app

# ===========================================
# OPTIONAL - Facebook Messenger Integration
# ===========================================
FB_PAGE_ACCESS_TOKEN=your_page_access_token
FB_VERIFY_TOKEN=your_webhook_verify_token
# App secret for webhook signature validation (recommended)
FB_APP_SECRET=your_app_secret
# Recipient PSIDs (comma-separated)
FB_RECIPIENT_PSIDS=psid1,psid2
